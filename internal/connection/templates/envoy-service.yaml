apiVersion: v1
kind: Service
metadata:
  name: {{ .AppName }}-envoy-service
spec:
  type: ClusterIP
  ports:
    - port: 8126
      targetPort: 8126
      protocol: TCP
      name: http
  selector:
    app: {{ .AppName }}-envoy